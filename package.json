{
  "name": "dgd",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "playwright:install": "playwright install chromium",
    "playwright:check": "playwright --version"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
